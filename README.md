This is filght booking application made with fully structured file.

`src` -> inside the src folder all the actual source code regarding the project will reside


Let's take a look inside the `src`

- `config` -> Inside this folder anything and everything regarding any configration setup is there

